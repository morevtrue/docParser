# Phase 2 — Тесты и проверки

**Статус:** ⏳ Ожидает выполнения

---

## Подготовка

1. Phase 1 завершена (Docker Compose работает, health-check проходит).
2. Файлы из `Основной набор/` доступны для тестирования.

---

## Чеклист

### API
- [ ] `POST /api/process` с корректными DOCX → 200 + JSON с извлечёнными данными
- [ ] `POST /api/process` с не-DOCX файлом → 400 с сообщением об ошибке
- [ ] `POST /api/process` без файлов → 400
- [ ] `GET /api/health` → 200 (не сломался)

### Парсинг запроса ТКП
- [ ] Название закупки извлечено корректно
- [ ] Номер и дата извлечены
- [ ] Сроки и требования извлечены
- [ ] Ненайденные поля → null + warning в отчёте

### Парсинг карточки поставщика
- [ ] Наименование, ИНН, КПП, ОГРН извлечены
- [ ] Адрес и контакты извлечены
- [ ] Банковские реквизиты извлечены
- [ ] Ненайденные поля → null + warning в отчёте

### Парсинг коммерческих условий
- [ ] Позиции товаров/услуг извлечены
- [ ] Цены извлечены
- [ ] Условия поставки и сроки извлечены
- [ ] Ненайденные поля → null + warning в отчёте

### Отчёт об обработке
- [ ] Все найденные поля перечислены с источником
- [ ] Все ненайденные поля перечислены как warnings
- [ ] Ошибки парсинга (если были) зафиксированы

### Классификация документов
- [ ] Система корректно определяет тип каждого загруженного документа

---

## Результаты тестирования

[Заполняется агентом после прохождения тестов.]
