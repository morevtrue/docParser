# Phase 1 — TESTS

## Чеклист проверок

| # | Проверка | Команда / способ | Ожидаемый результат | Статус |
|---|----------|-----------------|---------------------|--------|
| 1 | Сборка контейнеров | `docker compose up --build` | Оба контейнера запускаются без ошибок | ⬜ |
| 2 | Health-check бэкенда | `curl http://localhost:8000/api/health` | `{"status":"ok"}` | ⬜ |
| 3 | Фронтенд открывается | Браузер → `http://localhost:5173` | Страница-заглушка DocParser | ⬜ |
| 4 | Proxy работает | `curl http://localhost:5173/api/health` | `{"status":"ok"}` (через proxy) | ⬜ |
| 5 | Сеть между контейнерами | Логи backend при старте | Uvicorn запущен на 0.0.0.0:8000 | ⬜ |

## Как запустить

```bash
docker compose up --build
```

Затем открыть в браузере: http://localhost:5173
