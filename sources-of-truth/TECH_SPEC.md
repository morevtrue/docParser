# TECH_SPEC — Техническая спецификация DocParser

## Общая техническая концепция

Локальное веб-приложение с раздельным фронтендом и бэкендом, запускаемое через Docker Compose.

Ключевые принципы:
- простота и минимализм — MVP без overengineering;
- конфигурация через переменные окружения;
- устойчивость к изменениям формы входных документов;
- разделение логики парсинга, маппинга и генерации.

---

## Архитектурный стиль

**Monolith (два контейнера: frontend + backend)**

Коммуникация:
- Sync: HTTP/REST (frontend → backend API)
- Async: не требуется для MVP

---

## Технологический стек

### Backend

| Компонент          | Технология       | Причина выбора                                    |
|--------------------|------------------|---------------------------------------------------|
| Web-фреймворк      | FastAPI          | Async, автодокументация, Pydantic из коробки      |
| Парсинг DOCX       | python-docx      | Удобный доступ к параграфам, таблицам, ячейкам    |
| DTO / валидация    | Pydantic         | Типизированный промежуточный слой данных          |
| Архивация          | zipfile (stdlib) | Упаковка результатов в ZIP                        |

### Frontend

| Платформа | Технология          | Статус             |
|-----------|---------------------|--------------------|
| Web       | React + shadcn/ui   | ✅ Основной фокус  |

> **Важно:** для инициализации React + shadcn использовать официальный CLI и инструкцию:
> https://ui.shadcn.com/docs/installation
> Не писать конфиги (tailwind, components.json и т.д.) вручную — всё делается через `npx shadcn@latest init`.

### Инфраструктура

| Компонент       | Технология      |
|-----------------|-----------------|
| Контейнеризация | Docker Compose  |
| Frontend-сборка | Vite (dev server с proxy на бэкенд) |

---

## Структура проекта (целевая)

```
docparser/
├── docker-compose.yml
├── backend/
│   ├── Dockerfile
│   ├── requirements.txt
│   ├── app/
│   │   ├── main.py              ← FastAPI app, роуты
│   │   ├── parser/              ← Парсинг входных DOCX
│   │   │   ├── base.py          ← Базовый парсер
│   │   │   ├── customer_request.py  ← Парсер запроса ТКП
│   │   │   ├── supplier_card.py     ← Парсер карточки поставщика
│   │   │   └── commercial_terms.py  ← Парсер коммерческих условий
│   │   ├── models/              ← Pydantic DTO
│   │   │   └── schemas.py
│   │   ├── generator/           ← Генерация выходных DOCX
│   │   │   └── filler.py
│   │   └── mapping/             ← Маппинг: какие поля откуда берутся
│   │       └── field_map.py
│   └── templates/               ← Шаблоны (опционально, для fallback)
├── frontend/
│   ├── Dockerfile
│   ├── package.json
│   ├── vite.config.ts          ← proxy /api/* → backend:8000
│   ├── src/
│   │   ├── App.tsx
│   │   ├── components/
│   │   └── ...
└── README.md
```

---

## API-контракт

### POST /api/process
- Принимает: `multipart/form-data` с DOCX-файлами (входные документы + шаблоны).
- Возвращает: ZIP-архив с заполненными документами + JSON-отчёт.
- Ошибки: 400 если не DOCX, 422 если не удалось распарсить, 500 при внутренней ошибке.

### GET /api/health
- Возвращает: `{"status": "ok"}`

---

## Подход к парсингу

1. **Семантический поиск полей** — ищем по ключевым словам/фразам в тексте и таблицах, а не по индексам строк.
2. **Маппинг полей** — конфигурируемая таблица соответствий: "какое поле в каком документе искать" → "куда в шаблоне вставить".
3. **Fallback** — если поле не найдено семантически, пробуем по позиции в таблице (как дополнительный механизм, не единственный).
4. **Отчёт** — каждое поле логируется: найдено / не найдено / источник.

---

## Ограничения (важно для агента)

- ❌ не добавлять технологии без явной причины и согласования;
- ❌ не использовать внешние LLM API;
- ❌ не писать бизнес-логику во frontend;
- ❌ не хардкодить индексы строк/ячеек как единственный механизм парсинга;
- ✅ придерживаться rule-based подхода к парсингу;
- ✅ код минимальный, чёткий, явный;
- ✅ конфигурация вынесена наружу (env-переменные, маппинг полей).

---

## Итог

TECH_SPEC определяет **КАК** реализуется система.
Если BUSINESS_SPEC — это «зачем», то TECH_SPEC — это технический закон проекта.
